#!/bin/bash
# Constellation SessionStart Hook for Cline
# Generated by constellation-cli - DO NOT EDIT
#
# This script injects Constellation guidance at task start.
# It reads JSON input from stdin (ignored) and outputs JSON with contextModification.

cat << 'CONSTELLATION_EOF'
{
  "cancel": false,
  "contextModification": "You have Constellation access. `code_intel` is your PRIMARY tool for understanding this codebase—not an alternative, not a fallback, THE DEFAULT.\n\nMental model:\n- \"Where is X defined?\" → code_intel\n- \"What uses X?\" → code_intel\n- \"What would break?\" → code_intel\n- \"Find the string 'ERROR_CODE_123'\" → Grep\n\nGrep is for literal text. Everything else starts with code_intel."
}
CONSTELLATION_EOF
