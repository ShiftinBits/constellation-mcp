# Constellation MCP — E2E Test Results

**Date**: 2026-08-06
**Tester**: Claude Code (/mcp-regression)
**Procedure**: constellation-mcp/docs/E2E-TEST-PROCEDURE.md
**Index state**: c660dbc53571d7b25f7943b143373a9eb2f7cd5f

> **Revised after root-cause investigation.** Two tests originally scored PARTIAL
> ("stale fixture") were re-scored FAIL once the underlying defect was proven.
> See [Root Cause: Missing CALLS Edges](#root-cause-missing-calls-edges).

---

## Post-Fix Verification (2026-08-07) — BLOCKED, defects still reproduce

Attempted to re-verify the findings below against constellation-core's
`bug/SB-1103-code-graph-correctness` branch, which contains the intended fixes
(`1a1832ad` dedup AST traversal / stop faking CALLS caller ids, `38009d0d` widen
`CALLS_P2_FALLBACK` S3 receiver-type kind filter, `7826403d` stop tagging
object-literal method containers as class kind).

**Steps taken:**

1. Switched constellation-mcp to `main` (required — `constellation index --full`
   refuses to run when the checked-out branch doesn't match `constellation.json`'s
   configured `branch`), ran a clean full re-index, confirmed completion via
   `indexing-service` logs (`INDEX_AST job completed`, 81 files, 3028 symbols,
   `indexAttemptId: 59c58e7e-9758-4f12-8922-566dee8c2865`), then switched back.
2. Re-queried `getCallGraph({direction: 'incoming'})` for `createStructuredError`
   and `getDefaultConfig`: **still 0 callers for both**, versus the expected 6 and 5. `getCallGraph({direction: 'outgoing'})` on `registerQueryCodeGraphTool`
   (cyclomatic complexity 19, calls `createStructuredError` five times) also
   returned **0 callees** — the CALLS-edge gap is systemic, not limited to the two
   repro symbols.
3. Re-queried `searchSymbols({filterByKind: ['class']})`: still 30 results,
   hand-verified 1:1 against `grep -rnE "\bclass\s+[A-Za-z_$]..." src test` (30
   real class declarations, including 3 test-file `Foo: class Foo extends Error`
   object-property patterns; the two string-literal fake classes in
   `source-enrichment.test.ts:466` and `auto-return.test.ts:189` are correctly
   excluded). `getCapabilities().symbolCount` is unchanged at **2993** against the
   indexer's reported **3028** raw symbols — the same 35-symbol gap as the
   pre-fix run, meaning the anonymous-`<class>` object-literal defect has not
   measurably changed at the graph level either (the API-side count was already
   filtering these before the fix, so this metric alone can't fully confirm the
   graph-level fix; `symbolCount` vs raw indexer count is the best available
   proxy without direct Neo4j access).

**Root cause of the non-reproduction:** the running `intel-api` /
`indexing-service` Docker images were built at `2026-08-06T22:59:39Z` — **before**
the fix commits landed on core's branch (`1a1832ad` through `7826403d`/`355bbb5f`,
20:42–21:24 MDT = 02:42–03:24 UTC on 08-07, i.e. after the image build). Attempted
`npm run start:container:intel-api` in constellation-core to rebuild against the
fix branch; the build failed at `npm ci`:

```
npm error code ETARGET
npm error notarget No matching version found for @constellationdev/types@^1.3.7.
```

`355bbb5f` bumped core's `package.json` to `@constellationdev/types@^1.3.7`, but
`npm view @constellationdev/types versions` shows the latest published version is
still `1.3.6`. Rebuilding the containers against the fix branch requires either
publishing `@constellationdev/types@1.3.7` or hand-linking a local build inside
core's Dockerfile — both out of scope for constellation-mcp's work group.

**Conclusion:** this run reproduces the exact same defects as the original
2026-08-06 report (missing CALLS edges, unchanged `symbolCount`/raw-count gap).
It does **not** confirm the fixes work — it confirms the currently-deployed Core
containers do not yet contain them. **Follow-up required:** publish
`@constellationdev/types@1.3.7` (or otherwise get the fix branch built and
deployed to the shared dev Core stack), then re-run this same verification
(`getCallGraph` incoming counts for `createStructuredError`/`getDefaultConfig`,
`searchSymbols` class-kind count vs. hand-grep, `getCapabilities().symbolCount`
vs. indexer-reported symbol count) before this epic's core-side fixes can be
considered proven.

---

## Summary

| Metric      | Count |
| ----------- | ----- |
| Total tests | 124   |
| Passed      | 118   |
| Partial     | 4     |
| Failed      | 2     |
| Pass rate   | 95.2% |

## Results by Category

| Category               | Tests   | Pass    | Partial | Fail  |
| ---------------------- | ------- | ------- | ------- | ----- |
| Discovery Methods      | 14      | 13      | 1       | 0     |
| Dependency Methods     | 9       | 9       | 0       | 0     |
| Tracing Methods        | 9       | 7       | 0       | 2     |
| Impact Methods         | 9       | 9       | 0       | 0     |
| Architecture Methods   | 4       | 4       | 0       | 0     |
| Parameter Validation   | 11      | 11      | 0       | 0     |
| Error Handling         | 7       | 6       | 1       | 0     |
| Sandbox Security       | 17      | 17      | 0       | 0     |
| Edge Cases             | 8       | 8       | 0       | 0     |
| Combined Workflows     | 5       | 5       | 0       | 0     |
| Code Mode Specific     | 21      | 19      | 2       | 0     |
| Neo4j Cross-Validation | 10      | 10      | 0       | 0     |
| **TOTAL**              | **124** | **118** | **4**   | **2** |

---

## Failed Tests

### TC-TRACE-001: traceSymbolUsage - By Symbol ID

**Score**: FAIL (originally PARTIAL, re-scored)
**Expected**: `directUsageCount > 0` for `getDefaultConfig`.
**Actual**: `directUsageCount: 0`, `resultContext.reason: "no_matches"`.
**Neo4j**: `usageCount: 0`, `relationshipTypes: []`.
**Discrepancy**: The API and the graph agree, but both are wrong. `getDefaultConfig`
is called at five sites in the codebase (`src/index.ts:40`,
`src/tools/query-code-graph-tool.ts:148`, `src/client/error-factory.ts:49` and `:90`,
`src/client/error-mapper.ts:26`) against the definition at
`src/config/config-cache.ts:180`. The graph contains an `UnresolvedCall` node for
this callee, meaning the call site was captured and staged but never promoted to a
`CALLS` edge. Originally mis-scored as fixture staleness because API/Neo4j agreement
was mistaken for correctness.

### TC-TRACE-008: getCallGraph - Callers

**Score**: FAIL (originally PARTIAL, re-scored)
**Expected**: `callerCount > 0` for `createStructuredError`.
**Actual**: `hasCallers: true`, `callerCount: 0`.
**Neo4j**: `callerCount: 0`, `callerNames: []`.
**Discrepancy**: `createStructuredError` is invoked at six sites
(`src/code-mode/sandbox.ts:602`; `src/tools/query-code-graph-tool.ts:283, 311, 346,
377, 500`). The graph holds neither a `CALLS` edge nor an `UnresolvedCall` staging
node. Root-caused below.

---

## Partial Tests

### TC-DISC-012: getCapabilities - Project Status Check

**Score**: PARTIAL
**Expected**: API `symbolCount` equals the Neo4j symbol count.
**Actual**: `symbolCount: 2993`, `fileCount: 81`, `isIndexed: true`.
**Neo4j**: `symbolCount: 3028`, `fileCount: 81`.
**Discrepancy**: File counts match exactly. The 35-symbol gap equals the number of
synthetic `<anonymous@line:col>` symbols in the graph. The API filters them from its
total; raw Cypher counts them. Systematic and explained, but the stated equality does
not hold. See the object-literal defect below.

### TC-ERR-007: MEMORY_EXCEEDED and structuredContent (Documentation)

**Score**: PARTIAL
**Expected**: The documentation payload executes and returns the documented structure.
**Actual**: Rejected before execution:
`Code validation failed: Dangerous pattern detected: \bprocess\.`
**Discrepancy**: The procedure's own code block contains the string literal
`'Best-effort periodic heap checking via process.memoryUsage()'`. The pre-parse regex
sweep matches dangerous-global patterns inside string literals. The feature itself is
present and correct (`src/types/mcp-errors.ts:38`, `src/code-mode/sandbox.ts:96`,
heap check at `sandbox.ts:546-548`); what failed is the validator.

### TC-CODE-014: All Registry Methods Listed

**Score**: PARTIAL
**Expected**: `methodCount: 12`, `allPresent: true`, `hasDecisionGuide: true`, `hasReference: true`.
**Actual**: `methodCount: 14`, `missing: []`, all boolean assertions true.
**Discrepancy**: `listMethods()` now returns 14 entries — the 12 registry methods plus
`listMethods` and `help` themselves. Every substantive assertion passes; the procedure's
count constant is stale.

### TC-CODE-018: listMethods Query Filter (SB-257)

**Score**: PARTIAL
**Expected**: `allCount === 12`; filtered subset non-empty and smaller.
**Actual**: `allCount: 14`, `filteredCount: 3`
(`getDependencies`, `getDependents`, `findCircularDependencies`), `isSubset: true`.
**Discrepancy**: Same stale constant. Filtering behavior itself is correct.

---

## Root Cause: Missing CALLS Edges

The most serious finding. Traced end to end through the real pipeline and confirmed
reproducible on a clean full re-index.

### Symptom

Functions with many real call sites report zero callers. `getCallGraph({direction:
'incoming'})` returns `callerCount: 0` for a function called six times. This is a
confidently wrong answer, not an empty result.

### What was eliminated

Each of these was tested directly, not inferred:

| Stage                 | Method                                                                                     | Result                                                                  |
| --------------------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- |
| CLI parse + serialize | Ran the real `serializeAST` over the real 49KB `sandbox.ts`                                | All 193 call nodes serialized with callee identifier text intact        |
| Core extraction       | Replayed the serialized tree through `wrapSerializedNode` + the real `TypeScriptExtractor` | 16 rows for `createStructuredError`, 128 for `enrichWithSourceSnippets` |
| Call shape            | Matrix of bare/member callee × single/multi-line args × inside `try`/`catch`               | All shapes captured correctly                                           |
| Scope attribution     | Nested arrow callbacks                                                                     | Correctly attributed to the enclosing named symbol                      |
| P1/P2 routing         | `relationship-batcher.service.ts:509`                                                      | All callees route identically to the P2 cross-file path                 |
| Target ambiguity      | Candidate count per callee                                                                 | Each resolves to exactly one unique exported symbol                     |
| Incremental staleness | Clean `constellation index --full`                                                         | Reproduces exactly                                                      |

### Root cause

Instrumenting `Pass2OrchestratorService.runCallsPair` to dump every row entering the
P2 fallback produced a perfect correlation:

| Callee                     | Rows with real caller id | Rows with `global` | Graph result |
| -------------------------- | ------------------------ | ------------------ | ------------ |
| `validateAst`              | 1                        | 60                 | 1 CALLS edge |
| `addAutoReturn`            | 1                        | 36                 | 1 CALLS edge |
| `getProjectCapabilities`   | 1                        | 23                 | 1 CALLS edge |
| `createStructuredError`    | **0**                    | 64                 | **none**     |
| `enrichWithSourceSnippets` | **0**                    | 11                 | **none**     |

Two defects compound:

1. **Extractor loses the enclosing scope.** `typescript-extractor.ts:1584` builds rows
   with `callerId: currentScope || 'global'`. When `getCurrentScope()` returns empty it
   emits the sentinel string `global`. In this project 8669 P2 rows were produced and
   the overwhelming majority carry that sentinel (194 of 197 rows in the watched sample).

2. **The graph write discards them silently.** The P2 template opens with a
   non-optional match (`pass2-templates.ts:1245`):

   ```cypher
   MATCH (caller:Symbol {id: call.callerId, ...})
   ```

   No symbol has id `global`, so the row yields nothing. Both `FOREACH` branches — the
   one that creates the `CALLS` edge (line 1279) and the one that stages an
   `UnresolvedCall` for forensics (line 1291) — sit downstream of that match, so the row
   produces no edge _and_ no diagnostic record.

A call site therefore survives only if at least one of its duplicated rows happens to
retain a real scope id, which is why the pattern looked arbitrary.

### Second failure mode

`getDefaultConfig` (TC-TRACE-001) fails differently: its call rows _do_ reach the graph
and stage an `UnresolvedCall` node, but the later promotion passes never convert it to a
`CALLS` edge even though the target exists as an exported method in the same project.
Modes A (row discarded before staging) and B (staged but never promoted) are distinct
and both need fixing.

### Scale

The project holds roughly 108 CALLS edges per index generation despite containing 137
methods and 101 functions. Most call rows never survive the caller match, so call-graph
answers are broadly under-populated rather than occasionally wrong.

### Not fully isolated

Why `getCurrentScope()` returns empty for `sandbox.ts:602` but not `sandbox.ts:1401`
was not determined. Every structural difference reproduced in isolation retained scope
correctly.

---

## Other Defects

### Object literals extracted as anonymous `class` symbols

35 of the 65 `class` nodes are named `<anonymous@line:col>`. Verified against source:
`dangerous-patterns.ts:109` and `:126` are entries in a `PatternChecker[]` array
literal; `tsup.config.ts:4` and `:27` are config objects passed to `defineConfig`.
These are object literals, not classes. Query results are unaffected because the API
filters anonymous-named symbols (class searches return 30 while `function` 101 and
`method` 137 match Neo4j exactly), but the stored graph misrepresents the code and this
is the sole cause of the TC-DISC-012 count gap.

### Pattern checkers match inside string literals

The regex sweep that runs ahead of the parse-based validator does not distinguish code
from string contents, so any script mentioning `process.`, `eval(`, or `require(` in a
string or comment is refused. Demonstrated by TC-ERR-007. The parse-based validator
handles this correctly.

### `filterByKind` accepts unknown values silently

`filterByKind: ['bogusKind']` returns an empty result set with no error, while
`direction: 'sideways'` correctly fails with an enum validation error. A typo yields a
plausible-looking empty answer instead of a validation failure.

### Call-row inflation

The extractor emits 3416 call rows for 193 call sites in one file (~17x). The factor
tracks nesting depth as a power of two (8 rows for shallow calls, 128 for the deeply
nested `enrichWithSourceSnippets`), suggesting repeated subtree traversal. Every call
site also emits one row with the unusable `global` caller id.

---

## Procedure Staleness (not product defects)

- **Case sensitivity.** The procedure states the Core executor uses case-sensitive
  `CONTAINS` and instructs validators not to use `toLower()`. No longer true. `query: 'e'`
  returned 2510 against case-sensitive Cypher 2413 and case-insensitive 2510. The
  exported variant matched the same way (API 140, case-sensitive 92, case-insensitive
  140). Both TC-XVAL-001 and TC-XVAL-006 pass against case-insensitive ground truth.
- **`listMethods()` count.** Now 14, not 12 (TC-CODE-014, TC-CODE-018).
- **Weak Neo4j proxies.** Several validation queries diverge from the API for reasons
  that are query artifacts: depth traversals filter on `:File` and exclude `Package`
  targets; `count(dependent)` without `DISTINCT` inflates counts; the impact query walks
  `(File)-[:CONTAINS]->(ref)` and misses references originating from `File` nodes (7 of
  9 for `ConstellationClient`).
- **Cycle rotations.** `findCircularDependencies` reports 1 cycle where the procedure's
  Cypher reports 2 — the same two-file cycle traversed from each member. The API's
  deduplication is correct.

## Retracted Finding

An earlier draft flagged duplicate index generations (6056 symbol nodes for 3028
distinct ids) as a possible leak. **This is not a defect.** Generational writes with a
delayed sweep are the intended design. The reconciliation cron's `graph_attempt_orphan`
pass deletes non-active generations directly, runs every 30 minutes, and skips attempts
inside a 1-hour grace window (`RECONCILE_GRACE_MS`). The generations observed were
simply inside that window.

## Vacuous Passes

TC-DISC-005 passes because API `usageCount: 0` matches Neo4j `usageCount: 0` for
`getDefaultConfig` — but the true answer is five call sites. Any cross-validation test
whose ground truth is the same graph it is validating will agree with itself. Tests that
assert only API/Neo4j agreement cannot detect extraction or resolution defects; several
call-graph assertions in the procedure have this shape.

---

## Environment

- Constellation Core: localhost:3000 (Docker)
- Neo4j: localhost:7687
- Project ID: proj:adf45ed0a35649d19a7d70edaa9636b1
- Branch: main
- Index commit: c660dbc53571d7b25f7943b143373a9eb2f7cd5f
- Last indexed: 2026-08-06T23:13:35.589000000Z
- Indexed files: 81 (79 TypeScript, 2 JavaScript)
- Indexed symbols: 3028 (graph) / 2993 (API-reported)
